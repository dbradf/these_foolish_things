#!/usr/bin/env expect

set timeout 20
set name     [lindex $argv 0]
set user     [lindex $argv 1]
set password [lindex $argv 2]

spawn telnet $name

expect -re "\[lL]ogin:"
send "$user\n"

expect -re "\[pP]assword:"
send "$password\n"

interact
